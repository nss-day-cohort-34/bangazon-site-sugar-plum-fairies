@model Bangazon.Models.Order

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this order?
    All products will be removed from your cart..</h3>
<div>
    <hr />
    @*<dl class="row">
        <dd class = "col-sm-10">
            Items to be removed from cart: 
        </dd>
    </dl>
    <ul>
    @foreach (var op in Model.OrderProducts)
    {
        <li>@op.Product.Title</li>
    }
    </ul>*@

    <form asp-action="Delete">
        <input type="hidden" asp-for="OrderId" />
        <input type="submit" value="Delete" class="btn btn-danger" /> |
        <a asp-action="Details">Cancel</a>
    </form>
</div>
